apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mayanr.fullname" . }}-test-connection"
  labels:
    {{- include "mayanr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mayanr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
